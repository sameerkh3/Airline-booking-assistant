# Web framework
fastapi>=0.132.0
uvicorn[standard]==0.34.0

# Anthropic LLM SDK
anthropic==0.43.0

# Vector store for RAG (FAISS — compatible with Python 3.14; ChromaDB is not)
faiss-cpu==1.13.2
numpy>=1.24

# Embeddings model
sentence-transformers==3.4.1

# Environment variable management
python-dotenv==1.0.1

# MCP client SDK — used to connect to the Zapier MCP server for Gmail send_email
mcp>=1.0.0
